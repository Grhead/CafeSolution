<Page x:Class="CafeSolutionWPF.Pages.OrdersList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:viewmodels="clr-namespace:CafeSolutionWPF.ViewModels"
      d:DataContext="{d:DesignInstance Type=viewmodels:CookViewModel}"
      mc:Ignorable="d"
      Title="OrderLis" Height="650" Width="1100">
    <Grid>
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <ListBox
                Name="ListBoxOrders"
                Height="250" />
            <Button
                Content="Посмотреть заказ"
                HorizontalAlignment="Center"
                Click="ButtonBase_OnClick" />
        </StackPanel>
    </Grid>
</Page>